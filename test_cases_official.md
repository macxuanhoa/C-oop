# Application Testing Documentation

**System:** Education Centre System (WinForms)

| Test Case | Input | Expected Outcome | Actual Result | Status | Evidence |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Verify STT (Row Index) Stability** | 1. Open app.<br>2. Delete the row at STT 2. | The STT column automatically updates sequentially (1, 2, 3...) with no missing numbers or gaps. | The STT column automatically updates sequentially (1, 2, 3...) with no missing numbers or gaps. | Pass | Evidence 1 |
| **UI Mode Transition (Add/Edit)** | 1. Click out-of-focus "Add New" button directly while in 'Edit' mode. | The application instantly switches to 'Add' mode. Active button highlights with white border, others dim. | The application instantly switches to 'Add' mode. Active button highlights with white border, others dim. | Pass | Evidence 2 |
| **Dynamic Title Updating** | 1. Select user "John" and click "Edit Selected". | The Details panel title smoothly updates to `✏️ Editing: John`. | The Details panel title smoothly updates to `✏️ Editing: John`. | Pass | Evidence 3 |
| **Data Update Action (CRUD)** | 1. Select a row and click 'Edit Selected'.<br>2. Modify the Name field.<br>3. Save. | The modified name updates successfully in the DataGridView. Action traces directly through the Controller saving mechanism. | The modified name updates successfully in the DataGridView. Action traces directly through the Controller saving mechanism. | Pass | Evidence 4 |
| **Empty State Display** | 1. Enter random characters "xyzqwe" into the search box. | DataGridView displays a large overlay text: `🔍 No results matching your search.` | DataGridView displays a large overlay text: `🔍 No results matching your search.` | Pass | Evidence 5 |
| **Toast Notification Feedback** | 1. Successfully save a new record. | A green badge `✅ New record added successfully!` overlays the grid for exactly 3 seconds before hiding. | A green badge overlay appears for 3 seconds before hiding. | Pass | Evidence 6 |
| **UI State Lock (Integrity Constraint)** | 1. Click "Add New".<br>2. Observe the DataGrid and Delete button. | To guarantee data integrity during input, the grid is visually dimmed and destructive UI actions like Delete are disabled. | Grid and destructive tools become locked, forcing user to either save or cancel current state. | Pass | Evidence 7 |
| **Email Format Validation** | 1. Enter `john.doe` into the Email input.<br>2. Click Save in Add mode. | System rejects request. UI ErrorProvider displays a red icon indicating `Invalid email format`. | System rejects request. UI ErrorProvider displays a red icon indicating `Invalid email format`. | Pass | Evidence 8 |
| **Telephone Normalization** | 1. Enter `+1 (800) 123-4567` into Telephone.<br>2. Save record. | System successfully processes and normalizes the stored phone number to `+18001234567`. | System successfully processes and normalizes the stored phone number to `+18001234567`. | Pass | Evidence 9 |
| **Empty Mandatory Field (Name)** | 1. Leave the Name field blank.<br>2. Click Save. | Operation aborts. ErrorProvider flashes on the Name textbox demanding input. | Operation aborts. ErrorProvider flashes on the Name textbox demanding input. | Pass | Evidence 10 |
| **Polymorphism UI View (Dynamic Render)** | 1. Click Add New.<br>2. Switch the Role dropdown from `Student` to `Admin`. | The data entry TextBoxes automatically unmount/mount attributes bound to the selected Sub-Class (e.g., 3 Subjects hide, Salary and Working Hours appear). | UI structure intelligently readjusts relying on polymorphic properties of the models. | Pass | Evidence 11 |
| **Create Teacher (Salary Validation)** | 1. Set Role: `Teacher`.<br>2. Input `-500` under Salary.<br>3. Save. | Action rejected. Error indicates Salary must be a positive numeric value format. | Action rejected. Error indicates Salary must be a positive numeric value format. | Pass | Evidence 12 |
| **Update Admin Persistence** | 1. Double-click Admin record.<br>2. Change Job Type to `Part-time`.<br>3. Save. | The record saves to the backend. Grid instantly reflects the updated parameter `Type: Part-time`. | The record saves to the backend. Grid instantly reflects the updated parameter `Type: Part-time`. | Pass | Evidence 13 |
| **Filter by Role Isolation** | 1. Change Role filter dropdown from `All` to `Teacher`. | List safely repopulates to display exclusively Teacher profiles. STT numbering recounts from 1. | List safely repopulates to display exclusively Teacher profiles. STT numbering recounts from 1. | Pass | Evidence 14 |
| **Dataset Sorting Algorithm (LINQ)**| 1. Select `Name (Ascending)` from the "Sort By" drop-down. | Controller employs LINQ algorithm to re-orient the memory collection. Grid displays Data in A-Z order. | Data sets instantly reorder and display perfectly according to alphabetical increment constraints. | Pass | Evidence 15 |
| **Unique Email Constraint** | 1. Attempt to add a new person using an email already allocated on the grid. | A prompt intercepts indicating the email is redundant and restricts creation. | A prompt intercepts indicating the email is redundant and restricts creation. | Pass | Evidence 16 |
| **Direct Edit Shortcut** | 1. Double-click quickly on any non-header row in the grid. | App triggers direct Edit mode and shifts focus to the corresponding editable fields. | App triggers direct Edit mode and shifts focus to the corresponding editable fields. | Pass | Evidence 17 |
