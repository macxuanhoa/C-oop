# Tài Liệu Kiểm Thử Hệ Thống (Application Testing Documentation)

**Hệ thống:** Education Centre System (WinForms)

| Test Case (Trường Hợp Kiểm Thử) | Input (Dữ liệu đầu vào / Thao tác) | Expected Outcome (Kết quả Mong đợi) | Actual Result (Kết quả Thực tế) | Status (Trạng thái) | Evidence (Minh chứng) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **STT Tự động (Row Index)** | 1. Mở ứng dụng.<br>2. Xóa hàng đang ở STT số 2. | Cột STT tự động sắp xếp lại theo thứ tự (1, 2, 3...) không bị nhảy hay bỏ trống số nào. | Cột STT tự động cập nhật đúng thứ tự (1, 2, 3...) không bị nhảy số. | Pass | Evidence 1 |
| **Chuyển chế độ giao diện** | 1. Khi đang ở chế độ 'Edit', bấm trực tiếp vào nút "Add New" đang mờ. | Ứng dụng lập tức chuyển sang chế độ 'Add'. Nút đang chọn sáng lên với viền trắng, các nút khác mờ đi. | Ứng dụng lập tức chuyển sang chế độ 'Add'. Nút đang chọn sáng lên với viền trắng, các nút khác mờ đi. | Pass | Evidence 2 |
| **Tiêu đề Form Động** | 1. Chọn người tên "John" và bấm "Edit Selected". | Tiêu đề của bảng chi tiết bên trái chuyển mượt mà thành `✏️ Editing: John`. | Tiêu đề của bảng chi tiết bên trái chuyển mượt mà thành `✏️ Editing: John`. | Pass | Evidence 3 |
| **Thao tác Sửa Dữ Liệu (Update)** | 1. Chọn ai đó trên lưới rồi bấm 'Edit Selected'.<br>2. Sửa thông tin Tên.<br>3. Save. | Tên mới được cập nhật trên DataGridView. Hành động đi thẳng vào quy trình xử lý của Controller. | Tên người dùng trên bảng được đổi theo bộ chỉnh sửa thông tin. Lệnh lưu kích hoạt thành công. | Pass | Evidence 4 |
| **Hiển thị Rỗng (Empty State)** | 1. Nhập từ khóa ngẫu nhiên "xyzqwe" vào ô tìm kiếm. | Bảng dữ liệu tự động bị che phủ bởi câu thông báo lớn: `🔍 No results matching your search.` | Bảng dữ liệu đã bị che phủ bởi thông báo từ chối lớn: `🔍 No results matching your search.` | Pass | Evidence 5 |
| **Thông báo thành công (Toast)** | 1. Lưu mới thành công 1 người dùng. | Một dải màu xanh có chữ `✅ New record added successfully!` thả xuống lưới trong đúng 3 giây rồi biến mất. | Hiện dải màu xanh thông báo thành công trong 3 giây rồi tự biến mất hoàn toàn. | Pass | Evidence 6 |
| **Vô hiệu hóa UI khi thao tác (State Lock)** | 1. Bấm nút "Add New".<br>2. Quan sát khung lưới và nút Delete. | Để bảo vệ tính toàn vẹn (Integrity), lưới DataGrid bị làm mờ, nút Delete và Phân loại tìm kiếm bị khóa cứng. | Giao diện tạm thời đóng băng các thành phần ngoại vi để ép người dùng phải chọn Focus hoặc Cancel đi. | Pass | Evidence 7 |
| **Kiểm tra Định dạng Email** | 1. Nhập chữ `john.doe` vào ô Email.<br>2. Bấm Save. | Hệ thống từ chối lưu. Tại ô Email xuất hiện icon tam giác màu đỏ báo lỗi `Invalid email format`. | Hệ thống từ chối lưu và ép buộc điền lại, có icon chấm than đỏ hiển thị. | Pass | Evidence 8 |
| **Chuẩn hóa Số Điện Thoại** | 1. Nhập `+1 (800) 123-4567` vào ổ SDT.<br>2. Bấm Save. | Hệ thống xử lý thông minh và lưu chính xác kết quả vào CSDL dưới dạng chuẩn `+18001234567`. | Chi tiết thông tin sau khi lưu đã bị rút gọn các khoảng trắng và ký tự dư thừa sang `+18001234567`. | Pass | Evidence 9 |
| **Bỏ trống Trường bắt buộc** | 1. Cố tình bỏ rỗng ô Name.<br>2. Bấm Save. | Quá trình lưu bị ngăn chặn. Biểu tượng lỗi đỏ chớp nháy tại ô đòi hỏi bắt buộc phải có giá trị. | Quá trình lưu bị ngăn chặn, có thông báo báo lỗi hiện rõ yêu cầu nhập liệu. | Pass | Evidence 10 |
| **Hiển thị Form linh hoạt (Polymorphism UI)** | 1. Bấm Add New.<br>2. Ở mục Role, chuyển từ `Student` sang `Admin`. | Các khung nhập liệu (TextBox) tự động ẩn hiện tương ứng với thuộc tính riêng của Class đó (3 ô Môn học biến mất, hiện ra ô Lương và Giờ làm). | Giao diện tự động thay đổi TextBox một cách trơn tru tương thích với từng loại đối tượng. | Pass | Evidence 11 |
| **Teacher (Bắt lỗi âm Lương)** | 1. Cột Role: `Teacher`.<br>2. Phần Lương (Salary) gõ vào chữ `-500`.<br>3. Cố lưu. | Từ chối. Chương trình bắn lỗi ngăn chặn nhập lương bị số âm (phải là >= 0). | Từ chối và nhắc nhở không được nhập số tiền âm. | Pass | Evidence 12 |
| **Cập nhật Data Admin** | 1. Click đúp Admin.<br>2. Gỡ bỏ `Full-time`, chọn thành `Part-time`.<br>3. Bấm Save. | Dữ liệu cập nhật xuống Backend. Bảng Grid phản ứng ngay lập tức sang hiển thị `Type: Part-time`. | Việc thay đổi trạng thái sang Part-time đã hiện rõ ở cột Detailed Info. | Pass | Evidence 13 |
| **Cô lập dữ liệu Role (Filter)** | 1. Đổi mục Role Filtering từ mặc định `All` sang loại `Teacher`. | Danh sách bị xoá trắng và thay thế ngay lập tức bằng toàn bộ Teacher hiện có. STT đánh lại từ 1. | Danh sách làm mới chỉ còn Teacher và không bị lẫn ai khác. STT khôi phục chính xác. | Pass | Evidence 14 |
| **Tính năng Sắp Xếp (Sorting)**| 1. Chọn "Sort By" là mục `Name (Ascending)`. | Controller chạy phương thức sắp xếp chuỗi (LINQ) và đổ lại danh sách xuống Form từ A tới Z. | Lưới dữ liệu hiển thị đúng chuẩn thứ tự bảng chữ cái alphabet tăng dần từ trên xuống dưới. | Pass | Evidence 15 |
| **Khóa Email Trùng lặp** | 1. Cố ý tạo mới một người khác bằng tài khoản email mà đã tồn tại từ trước ở dưới bảng. | Hộp thoại thông báo đỏ (Popup) nhảy dựng lên báo cho người dùng biết lỗi trùng Email không thể diễn ra. | Báo lỗi nhắc nhở trùng lặp trực diện và hủy quá trình thay đổi. | Pass | Evidence 16 |
| **Double Click phím tắt** | 1. Ấn nhanh Double-click thần tốc lên bất kỳ ai trong DataGridView. | UI đọc dữ liệu người bị nhấn và chủ động thả ra form Edit trên bảng nhập liệu bên góc trái. | Lập tức đi vào chế độ Edit với dữ liệu chờ sẵn trên hộp hiển thị. | Pass | Evidence 17 |
